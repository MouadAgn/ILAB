{% extends 'base.html.twig' %}

{% block title %}Create New Appointment{% endblock %}

{% block body %}
    <h1>Create New Appointment</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn">Create</button>
    {{ form_end(form) }}

    <a href="{{ path('appointment_index') }}">Back to list</a>
{% endblock %}
